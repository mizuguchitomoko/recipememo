<body>
  <container>
    <div class="row">
      <div class="col-xs-6 col-xs-offset-3">
        <table class="table">
        <h1>FAVORITES</h1>
          <thead>
            <tr>
              <th></th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <% @recipes.each do |recipe| %>
          <tbody>
            <tr>
              <td>
                <%= link_to recipe_path(recipe) do %>
                <%= attachment_image_tag favorite.recipe, :image, :fill, 150, 100 %>
                <% end %>
              </td>
              <td>
                <%= recipe.created_at.strftime('%Y/%m/%d') %>投稿<br>
                <b><%= favorite.recipe.recipe_title %></b><br>
                <%= favorite.recipe.recipe_food %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>

      </div>
    </div>
  </container>
</body>